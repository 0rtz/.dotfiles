# shellcheck shell=bash
_my_git_create_branch_remote() {
	local cur="${COMP_WORDS[COMP_CWORD]}"

	case "$COMP_CWORD" in
		1) COMPREPLY=() ;;
		2) COMPREPLY=($(compgen -W "$(git remote 2>/dev/null)" -- "$cur")) ;;
	esac
}
complete -F _my_git_create_branch_remote my-git-create-branch-remote
