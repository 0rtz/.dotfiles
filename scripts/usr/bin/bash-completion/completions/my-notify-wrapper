# shellcheck shell=bash
_my_notify_wrapper() {
	local cur="${COMP_WORDS[COMP_CWORD]}"

	if (( COMP_CWORD == 1 )); then
		COMPREPLY=($(compgen -c -- "$cur"))
	fi
}
complete -F _my_notify_wrapper my-notify-wrapper
